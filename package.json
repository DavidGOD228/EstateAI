{
  "name": "estateai",
  "private": true,
  "version": "0.1.0",
  "description": "EstateAI - real estate property assistant with AI features (take-home MVP)",
  "scripts": {
    "dev": "pnpm --parallel --filter @estateai/web --filter @estateai/api run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=20"
  }
}
